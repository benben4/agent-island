[package]
name = "agent_monitor_core"
version = "0.1.0"
edition = "2021"

[dependencies]
dirs = "6.0"
regex = "1.11"
rusqlite = { version = "0.32", features = ["bundled"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
walkdir = "2.5"

[dev-dependencies]
tempfile = "3.15"
